[package]
name = "global_runtime-crate"
version.workspace = true
edition.workspace = true

[lib]
name = "global_runtime"
crate-type = ["cdylib", "rlib"]

[dependencies]
neural-core = { path = "../neural_core" }
